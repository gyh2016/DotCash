import { PageCard } from '@/shared/components/PageCard';

export const SettingsPage = () => {
  return (
    <PageCard>
      <h2>设置</h2>
      <p>本页将用于 JSON 导入导出、汇率偏好和数据恢复等功能。</p>
    </PageCard>
  );
};
